<div class="well form">
  <div class="form-group">
    <label for="realms">Realm</label>
    {{view "select" id="realms" class="form-control" content=realms value=realm}}
  </div>
  <div class="checkbox">
    <label>{{input type="checkbox" checked=onlyMax}} Only show max level characters</label>
  </div>
</div>
<div class="characters-list">
  {{#each character in filtered itemController="character"}}
    <div class="character">
      <h3>{{character.model.name}} <small>{{character.model.level}} {{character.model.realm}}</small></h3>
      <img class="character" {{bind-attr src="character.model.image_url"}}/>
      <p>{{character-race character.model.race_id}} {{character-class character.model.class_id}}</p>
      <p class="links">
        <a {{bind-attr href="character.armoryUrl"}}><img src="images/wow.png"/></a>
        <a {{bind-attr href="character.wowheadUrl"}}><img src="images/wowhead.png"/></a>
        <a {{bind-attr href="character.robotUrl"}}><img src="images/robot.png"/></a>
      </p>
    </div>
  {{else}}
    <div class="empty">
      <div class="alert alert-info">No {{#if onlyMax}}max level {{/if}}characters</div>
    </div>
  {{/each}}
</div>
