import {
  characterFaction
} from 'coretheloothound/helpers/character-faction';

module('CharacterFactionHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = characterFaction(42);
  ok(result);
});
